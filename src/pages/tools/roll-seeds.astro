---
// Roll Seeds 离线助记词生成器
// 通过 Vite ?raw 在构建期把自包含 HTML 内联进来，不依赖 fs/cwd，Cloudflare 环境更稳。
import toolHtml from '../../tools/roll-seeds.html?raw';

const clean = toolHtml.replace(/^\s*<!DOCTYPE[^>]*>/i, '');
---
<Fragment set:html={clean} />
