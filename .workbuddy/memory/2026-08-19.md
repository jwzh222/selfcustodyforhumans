# 2026-08-19

## for-mom 章节重构（导航可点击化）
- 需求：for mom 三节改名 1.创建钱包 / 2.备份钱包 / 3.使用钱包，每节标题可点击打开对应 md 主页。
- 改了 4 处：
  - `src/data/nav.ts`：`NavGroup` 加 `href?` 字段；for-mom 三节改为带 href 的分组（`create-wallet`/`backup-wallet`/`use-wallet`，原 `save-seed` 重命名为 `backup-wallet`）；子页 good-passphrase、roll-seeds、paper、metal、old-phone-electrum 保留；`FLAT` 生成时把分组主页也入列（供 prevNext/面包屑用）；`sectionsFor` 增加 `isGroupMain` 判断避免面包屑重复显示分组名。
  - `src/components/Sidebar.astro`：小节标题由 `<div>` 改为 `<a class="subsection--link">`（有 href 时），并按当前路由高亮。
  - `src/styles/global.css`：新增 `.subsection--link` hover 与 `.subsection.is-active` 样式。
- 路由约定：md 文件路径 = 路由。`create-wallet.md` 实际内容是「投骰子生成助记词」，标题与 roll-seeds 有重叠，属用户内容，未改动。
- 验证：`astro build` 通过，for-mom 8 个路由全部生成；`dist/index.html` 中三节标题均为带 href 的可点击链接。
- 备注：构建末尾有 safe-delete 守卫提示删除 `dist/.prerender/`（临时目录）被拦截，不影响产物，可忽略。

## for-mom 导航改为嵌套分组（第二轮）
- 需求：三节去掉前缀数字（创建钱包/备份钱包/使用钱包）；创建钱包下分两小节 1.助记词（再分 1.助记词是高风险环节、2.投骰子很快）、2.passphrase。
- 用户选择：新建两篇空白占位 md（不拆 roll-seeds.md）。passphrase 复用现有 good-passphrase.md（leaf id 用 good-passphrase，label 显示「2. passphrase」）。
- 改动：
  - `src/data/nav.ts`：`NavGroup.children` 类型改为 `(NavGroup | NavLeaf)[]`（支持嵌套）；`FLAT` 改递归 `collect()`；`sectionsFor` 改为沿路由逐层 tree-walk；for-mom 结构改为 create-wallet{ mnemonic{ mnemonic-risk, dice-fast }, good-passphrase }。
  - `src/components/Sidebar.astro`：支持二级嵌套分组渲染（group→group→leaf）+ 各级高亮。
  - `src/styles/global.css`：新增 `.subsection-nested` 间距样式。
  - 新建 `src/content/docs/for-mom/create-wallet/mnemonic/mnemonic-risk.md` 与 `dice-fast.md`（TEMPLATE 占位，待填内容）。
- 验证：`astro build` 通过；嵌套路由 for-mom/create-wallet/mnemonic/{mnemonic-risk,dice-fast} 生成成功；`dist/index.html` 侧边栏层级与 href 均正确。
- 待办：roll-seeds.md 现已不在导航中（孤儿页，仍会构建），用户后续决定是否删除或迁移内容。

